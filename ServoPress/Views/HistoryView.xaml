<UserControl x:Class="ServoPress.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ServoPress.Models"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1050">
    <!-- 
    子界面 4: 历史数据
    -->
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- 筛选栏 -->
            <RowDefinition Height="*"/>
            <!-- 数据区 -->
        </Grid.RowDefinitions>

        <!-- 筛选栏 (背景已修改) -->
        <Border Grid.Row="0" Background="#FFFAFAFA" BorderBrush="#FFD0D0D0" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="开始时间:" VerticalAlignment="Center" Margin="5"/>
                <DatePicker Margin="5"/>
                <TextBlock Text="结束时间:" VerticalAlignment="Center" Margin="15,5,5,5"/>
                <DatePicker Margin="5"/>
                <TextBlock Text="程序号:" VerticalAlignment="Center" Margin="15,5,5,5"/>
                <TextBox Width="100" Margin="5"/>
                <TextBlock Text="结果:" VerticalAlignment="Center" Margin="15,5,5,5"/>
                <ComboBox Width="100" Margin="5" SelectedIndex="0">
                    <ComboBoxItem Content="全部"/>
                    <ComboBoxItem Content="OK"/>
                    <ComboBoxItem Content="NOK"/>
                </ComboBox>
                <Button Content="查 询" Margin="20,5,5,5"/>
            </StackPanel>
        </Border>

        <!-- 数据区 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <!-- 历史列表 -->
                <ColumnDefinition Width="1.5*"/>
                <!-- 选中曲线 -->
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="历史记录">
                <DataGrid>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="时间" Binding="{Binding Timestamp}" Width="*"/>
                        <DataGridTextColumn Header="程序号" Binding="{Binding Program}" Width="Auto"/>
                        <DataGridTextColumn Header="结果" Binding="{Binding Result}" Width="Auto"/>
                        <DataGridTextColumn Header="最大压力" Binding="{Binding MaxForce}" Width="Auto"/>
                    </DataGrid.Columns>
                    <!-- 示例数据 -->
                    <DataGrid.ItemsSource>
                        <x:Array Type="{x:Type local:HistoryItem}">
                            <local:HistoryItem Timestamp="2025/11/06 10:30:15" Program="MP101" Result="OK" MaxForce="450.2"/>
                            <local:HistoryItem Timestamp="2025/11/06 10:30:18" Program="MP101" Result="OK" MaxForce="451.1"/>
                            <local:HistoryItem Timestamp="2025/11/06 10:30:21" Program="MP101" Result="NOK" MaxForce="320.0"/>
                        </x:Array>
                    </DataGrid.ItemsSource>
                </DataGrid>
            </GroupBox>

            <GroupBox Grid.Column="1" Header="历史曲线 (2025/11/06 10:30:21)">
                <!-- 背景已修改 -->
                <Border Background="#FFFAFAFA" BorderBrush="#FFD0D0D0" BorderThickness="1" CornerRadius="4" Padding="10">
                    <Grid>
                        <TextBlock Text="选中的历史曲线" FontSize="20" Foreground="#FFA0A0A0" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                    </Grid>
                </Border>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>