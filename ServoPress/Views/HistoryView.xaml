<UserControl x:Class="ServoPress.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:oxy="http://oxyplot.org/wpf" 
             xmlns:vm="clr-namespace:ServoPress.ViewModels"
             xmlns:converters="clr-namespace:ServoPress.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">

    <UserControl.DataContext>
        <vm:HistoryViewModel/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <converters:BoolToResultConverter x:Key="BoolToResultConverter"/>
    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFFAFAFA" TextElement.FontSize="16" BorderBrush="#FFD0D0D0" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="开始时间:" VerticalAlignment="Center" Margin="5"/>
                <DatePicker Margin="5" SelectedDateFormat="Long" SelectedDate="{Binding StartDate}"/>

                <TextBlock Text="结束时间:" VerticalAlignment="Center" Margin="15,5,5,5"/>
                <DatePicker Margin="5" SelectedDateFormat="Long" SelectedDate="{Binding EndDate}"/>

                <TextBlock Text="产品型号:" VerticalAlignment="Center" Margin="15,5,5,5"/>
                <TextBox Width="100" Margin="5" Text="{Binding ProductModelKeyword}"/>

                <TextBlock Text="工站:" VerticalAlignment="Center" Margin="15,5,5,5"/>
                <ComboBox Width="100" Margin="5" SelectedIndex="{Binding SelectedStationIndex}">
                    <ComboBoxItem Content="全部"/>
                    <ComboBoxItem Content="1"/>
                    <ComboBoxItem Content="2"/>
                    <ComboBoxItem Content="3"/>
                    <ComboBoxItem Content="4"/>
                </ComboBox>

                <TextBlock Text="结果:" VerticalAlignment="Center" Margin="15,5,5,5"/>
                <ComboBox Width="100" Margin="5" SelectedIndex="{Binding SelectedResultIndex}">
                    <ComboBoxItem Content="全部"/>
                    <ComboBoxItem Content="OK"/>
                    <ComboBoxItem Content="NG"/>
                </ComboBox>

                <Button Content="查 询" Margin="20,5,5,5" Command="{Binding SearchCommand}" Width="80"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="历史记录">
                <DataGrid ItemsSource="{Binding HistoryRecords}" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False" 
                          IsReadOnly="True"
                          SelectedItem="{Binding SelectedRecord}">

                    <DataGrid.InputBindings>
                        <MouseBinding Gesture="LeftDoubleClick" 
                                      Command="{Binding ShowCurveCommand}" 
                                      CommandParameter="{Binding SelectedRecord}"/>
                    </DataGrid.InputBindings>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="工站" Binding="{Binding StationId}" Width="40" />
                        <DataGridTextColumn Header="产品型号" Binding="{Binding ProductModel}" Width="70"/>
                        <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="160"/>
                        <DataGridTextColumn Header="结果" Binding="{Binding ResulEnd,Converter={StaticResource BoolToResultConverter}}" Width="45"/>
                        <DataGridTextColumn Header="判定" Binding="{Binding ResultText}" Width="200"/>
                        <DataGridTextColumn Header="最小压力" Binding="{Binding MaxForce}" Width="80"/>
                        <DataGridTextColumn Header="最大压力" Binding="{Binding MaxForce}" Width="80"/>
                        <DataGridTextColumn Header="最终压力" Binding="{Binding EndForce}" Width="80"/>
                        <DataGridTextColumn Header="最小位移" Binding="{Binding MinPosition}" Width="80"/>
                        <DataGridTextColumn Header="最大位移" Binding="{Binding MaxPosition}" Width="80"/>
                        <DataGridTextColumn Header="最终位移" Binding="{Binding EndPosition}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <GroupBox Grid.Column="1" Header="曲线详情">
                <Border Background="#FFFAFAFA" BorderBrush="#FFD0D0D0" BorderThickness="1" CornerRadius="4" Padding="5">
                    <oxy:PlotView Model="{Binding CurveModel}"/>
                </Border>
            </GroupBox>
        </Grid>
    </Grid>
</UserControl>