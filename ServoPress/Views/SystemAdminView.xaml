<UserControl x:Class="ServoPress.Views.SystemAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ServoPress.Models"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1050">
    <!-- 
    子界面 5: 系统管理
    -->
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <GroupBox Header="程序管理 (Program Management)">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <!-- ListBox 背景已修改 -->
                    <ListBox Height="150" Background="White" BorderBrush="#FFD0D0D0">
                        <ListBoxItem Content="MP101_PartA"/>
                        <ListBoxItem Content="MP102_PartB"/>
                        <ListBoxItem Content="MP200_Bearing"/>
                    </ListBox>
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <Button Content="加载到系统" Margin="5"/>
                        <Button Content="另存为..." Margin="5"/>
                        <Button Content="删除" Margin="5"/>
                        <Button Content="新建" Margin="5"/>
                    </StackPanel>
                </Grid>
            </GroupBox>
            <GroupBox Header="用户管理">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="当前用户: Operator" VerticalAlignment="Center" FontSize="16" Margin="5"/>
                    <Button Content="切换用户" Margin="20,5,5,5"/>
                    <Button Content="权限管理" Margin="5"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>

        <GroupBox Grid.Column="1" Header="总线与IO 控制">
            <TextBlock Text="用于配置与 PLC 的 IO 映射、Profinet/EtherCAT 等总线通信..." FontSize="14" Foreground="#FF666666" TextWrapping="Wrap"/>
            <!-- 这里通常会放置一个复杂的 DataGrid 或配置界面来映射 IO 点和总线地址 -->
        </GroupBox>
    </Grid>
</UserControl>