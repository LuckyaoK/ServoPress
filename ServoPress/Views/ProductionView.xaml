<UserControl x:Class="ServoPress.Views.ProductionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ServoPress.Models"
             xmlns:views="clr-namespace:ServoPress.Views"
             xmlns:viewmodels="clr-namespace:ServoPress.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1050">

    <!-- 
    子界面 1: 主页 (操作界面)
    已修改为 4 工位布局
    -->


    <Grid Margin="5">
        <ItemsControl ItemsSource="{Binding Stations}">

            <!-- 3. 定义布局：使用一个 2x2 的均匀网格 -->
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="2" Rows="2" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <!-- 4. 定义模板：告诉它每个 "StationViewModel" 
                 都应该显示为一个 "StationView" UserControl -->

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <views:StationView DataContext="{Binding}" Margin="5" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>

        </ItemsControl>
    </Grid>
</UserControl>